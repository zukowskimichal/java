package pl.dev.java.essentials.pattern.singleton;

/**
 * Java Enum solves problem of creating single instance as java enforce that
 * enum is instantiated only once
 */
public enum SingletonEnum {

    INSTANCE;

    public static void singletonMethod() {

    }
}
